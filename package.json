{
  "name": "webgl-fractals",
  "version": "1.0.0",
  "scripts": {
    "watch": "watchify src/javascript/fractal.js -o public/bundle.js -t [ babelify --presets es2015 ]",
    "build": "browserify src/javascript/fractal.js -o public/bundle.js -t [ babelify --presets es2015 ]"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "es6ify": "^1.6.0",
    "babelify": "^7.2.0",
    "babel-preset-es2015": "^6.6.0",
    "rx": "^4.1.0"
  }
}
